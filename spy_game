# a function that takes in a list of integers and returns True if it contains 007 in order

def spy_game(nums):
    code = []
    for i in nums:
        if i in (0,7):
            code.append(i)
        continue
    if code == [0,0,7]:
        return True
    else:
        return False
